
//Variavéis que nunca foram declaradas vão gerar erros de "is not defined"

//Undefined nos diz que declaramos uma variável, porém, nunca atribuímos um valor

//null significa que a variável criada naquele momento não tem valor nenhum

var valor = 2 // variável definida
console.log(valor)

nomeAluno // identificador que nunca foi declarado como uma variável, nunca abriu espaço na memória do computador; por isso, o vscode vai gerar o erro de 'is not defined'


var idade = null // variável com valor null, mostrando que não tem nenhum valor específico, porém, foi inicializada


var nomeFuncionario // Variável criada, porém, não inicializada; por isso, o vscode vai nos retorna-lá como undefined





